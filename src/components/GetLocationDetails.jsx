import React from 'react'

const GetLocationDetails = ({longitude,latitude}) => {
    
  return (
    <>The location cordinates are {longitude} and {latitude}</>
  )
}

export default GetLocationDetails